package com.devicestats.android;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v7.app.AppCompatActivity;

public class WallpaperStatsActivity extends AppCompatActivity {

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        /*
        * Toggle full screen when taking screen shot.
        * maybe showing multicolour horizontal progress bar?
        *
        * INFO
        * IP, Mac, current security patch level, Google play services version
        * Manufacturer/model
        * SDK version
        */
    }
}
